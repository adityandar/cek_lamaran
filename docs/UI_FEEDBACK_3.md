1. ✅ ~~MOBILE - buka popup pertama ada yang ketutupan~~ → Dialog `max-h-[85vh] overflow-y-auto`.
2. ✅ ~~MOBILE - Filter dan sort sangat jelek~~ → Stack vertikal (`flex-col sm:flex-row`), horizontal scroll (`overflow-x-auto`), font lebih kecil di mobile (`text-[11px] sm:text-xs`).
3. ✅ ~~MOBILE - navbar berantakan overflow~~ → Ikon-only di mobile (`hidden sm:inline` untuk teks), logo CekLamaran `hidden sm:inline`. Email `truncate max-w-[120px]`. Padding `px-4 sm:px-6`.
4. ✅ ~~MOBILE - button homepage padding kebesaran~~ → `px-6 py-2.5 sm:px-8 sm:py-3`, teks `text-base sm:text-lg`. Tombol stack vertikal (`flex-col sm:flex-row`). Hero padding dikurangi (`pt-16 sm:pt-24`).
