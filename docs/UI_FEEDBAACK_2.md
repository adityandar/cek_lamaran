1. ✅ ~~Redundant status di job card (badge kiri + dropdown kanan)~~ → Badge kiri dijadikan `select` langsung, tidak ada dropdown terpisah di kanan. Cukup klik badge untuk ganti status. Delete button jadi satu-satunya action di kanan.
2. ✅ ~~Pindah toggle List/Kanban di atas filter/sort~~ → View toggle di row sendiri (atas), filter + sort di row bawah.
3. ✅ ~~Filter dan sort tidak konsisten~~ → Filter dan sort sekarang pakai style yang sama (`chipBase`/`chipActive`/`chipInactive`). Bedanya hanya label: "Filter" vs "Urut".
